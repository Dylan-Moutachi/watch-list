<h1>New list</h1>
<%= simple_form_for [@list] do |f| %>
  <%= f.input :name, collection: List::CATEGORIES, prompt: "Choose a category" %>
  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>
